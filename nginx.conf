server {
    listen 8080;
    server_name localhost;

    location / {
        proxy_pass http://localhost:8000;  # Replace with the address your Django app is running on
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}